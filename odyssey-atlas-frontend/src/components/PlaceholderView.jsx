import React from 'react';

// Placeholder component for other views
const PlaceholderView = ({ title }) => (
    <div className="p-10">
        <h2 className="text-4xl font-bold font-serif text-gray-800">{title}</h2>
        <p className="mt-4 text-lg text-gray-600">This section is under construction. Content will be available soon!</p>
    </div>
);

export default PlaceholderView;
